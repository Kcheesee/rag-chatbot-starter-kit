// Validated environment schema (zod). Phase 1 placeholder.
// The real provider-aware schema — the ONLY place process.env is read —
// is implemented in Phase 7 and consumed by every app at startup.
export const ENV_SCHEMA_VERSION = "0.1.0";
