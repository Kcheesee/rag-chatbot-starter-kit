{
  "name": "@rag-chat-agent/llm-adapters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed LLM + embedding provider adapters (Anthropic, OpenAI, Bedrock, Vertex, Azure, GovCloud, internal mTLS) behind a single factory.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  }
}
